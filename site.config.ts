type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "Akiyaz",
  site_description: "An adventure like no other",
  site_domain: "https://akiyaz.io",
};
